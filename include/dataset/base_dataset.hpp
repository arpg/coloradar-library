#ifndef BASE_DATASET_HPP
#define BASE_DATASET_HPP

#include "dataset/base_dataset.h"


namespace coloradar {

// template<typename RunT>
// std::vector<std::shared_ptr<RunT>> Dataset::getRuns() const {
//     std::cout << "Getting runs from dataset" << std::endl;
//     std::vector<std::shared_ptr<RunT>> values;
//     values.reserve(runs_.size());
//     for (const auto& kv : runs_) {
//         values.push_back(kv.second);
//     }
//     std::cout << "Got " << values.size() << " runs from dataset" << std::endl;
//     return values;
// }

}

#endif